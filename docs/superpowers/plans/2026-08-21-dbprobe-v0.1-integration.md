# dbprobe v0.1 Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Consolidate the independently verified v0.1 feature branches into one integration branch without losing architecture boundaries or duplicating portable rules.

**Architecture:** `integration-v0.1` is based on the verified Foundation Runtime head. Features are applied in dependency order as source checkpoints rather than via pull requests so exhausted GitHub Actions quota is not consumed. Overlapping files are resolved by preserving both contracts; no feature branch is force-rewritten.

**Tech Stack:** Go 1.25 target, GitHub source branches, existing local Go harnesses, Docker MySQL acceptance when environment access returns.

**Spec:** `docs/superpowers/specs/2026-08-21-dbprobe-v0.1-architecture-design.md`

## Global Constraints

- Never merge directly to `main` from this integration work.
- `internal/core`, `internal/app`, SDK and generic surfaces remain free of concrete database drivers/adapters except the CLI composition root.
- Generic `core.*` findings exist exactly once in core; adapters must not duplicate them after consolidation.
- MySQL SQL/driver code remains under `adapters/mysql`.
- Temporal history remains engine-agnostic; SQLite persistence stays pending until dependency resolution is available.
- No new PRs are opened during consolidation while Actions quota is exhausted.
- Every overlap is resolved intentionally; never choose a source branch wholesale when it would discard already integrated behavior.

---

### Task 1: Bounded collection planner

**Source:** `feat/concurrent-collection-planner`

**Files:**
- Replace: `internal/core/collection/planner.go`
- Add: `internal/core/collection/concurrency_test.go`

- [ ] Apply the verified bounded-concurrency planner.
- [ ] Preserve default max concurrency `4`, `WithMaxConcurrency`, one counter sample wait, cancellation and deterministic sorted output.
- [ ] Keep existing Foundation planner tests unchanged.
- [ ] Verify focused planner normal/race suite when local source is available.

### Task 2: Generic core findings

**Source:** `feat/generic-core-findings`

**Files:**
- Add: `internal/core/findings/*`
- Modify: `internal/app/inspect/service.go`
- Add: `internal/app/inspect/generic_findings_test.go`

- [ ] Run generic core rules before adapter-native rules.
- [ ] Preserve engine-agnostic evidence grouping.
- [ ] Keep exactly one `core.connection_saturation` implementation after MySQL is added.

### Task 3: Temporal intelligence

**Source:** `feat/temporal-intelligence`

**Files:**
- Add: `internal/core/temporal/*`
- Add: `internal/platform/baseline/*`
- Add: `internal/app/diff/*`
- Add: diff JSON/terminal surfaces.
- Merge: `internal/app/inspect/service.go` and history support.
- Merge: `test/architecture/dependencies_test.go`.

- [ ] Preserve generic core findings wiring while adding optional `WithHistory` snapshot persistence.
- [ ] Preserve privacy filtering for query-text sensitivity.
- [ ] Preserve reset-aware diff, bounded event intervals, duplicate-identity suppression and query regression.

### Task 4: MySQL production adapter

**Source:** `feat/mysql-adapter-mvp`

- [ ] Apply adapter/config/runtime/collectors/findings/tests, Go module dependency, CLI registry and integration fixtures.
- [ ] Remove MySQL-local `core.connection_saturation` implementation because Task 2 owns it.
- [ ] Preserve MySQL-native findings only.
- [ ] Merge Makefile/README changes without removing Foundation smoke checks or temporal documentation.

### Task 5: MySQL schema fingerprint

**Source:** `feat/mysql-schema-fingerprint`

- [ ] Apply the six-surface versioned fingerprint collector and capability.
- [ ] Preserve 1 MiB field, 4 MiB record, 100k rows/group and 64 MiB total metadata limits.
- [ ] Merge capability changes with later plan-explain capability changes rather than overwriting either.

### Task 6: Safe plan-only EXPLAIN

**Source:** `feat/plan-explain`

- [ ] Apply optional SDK `PlanExplainer`, application/surfaces/CLI and MySQL implementation.
- [ ] Preserve read-only transaction, rollback, timeout, statement limits and sanitizer allowlists.
- [ ] Preserve schema-fingerprint capability from Task 5 while changing the explain probe to the actual `EXPLAIN FORMAT=JSON SELECT 1` surface.

### Task 7: MongoDB abstraction validation

**Source:** `test/mongodb-abstraction-probe`

- [ ] Apply only test/architecture probe artifacts not already present.
- [ ] Keep it test-only; do not create production MongoDB driver dependency.
- [ ] Verify Mongo-native query shape/WiredTiger semantics still exercise generic temporal/core contracts.

### Task 8: Consolidated verification

- [ ] Run dependency-free unit/race packages locally where possible.
- [ ] Run full Go 1.25 `go mod tidy`, `gofmt`, `go vet`, `go test ./...`, `go test -race ./...` when dependency/toolchain access returns.
- [ ] Run MySQL 8.0.46 + 8.4.11 Docker matrix when Docker is available.
- [ ] Do not mark integration branch merge-ready until all environment-dependent gates pass.
