# dbprobe v0.1 release-readiness

## Purpose

Prepare the promoted v0.1 code line for an explicit `v0.1.0` release without making tag creation or release publication a side effect of ordinary development merges.

This document records the release contract. It does not authorize creation of a tag or GitHub Release.

## Release contract

A release candidate must satisfy all of the following before publication:

- the release tag uses canonical `vMAJOR.MINOR.PATCH` form;
- the tag resolves to a commit contained in `main` history;
- annotated and lightweight tags are both bound to the dereferenced commit, not a tag-object SHA;
- the tagged source reruns permanent `make ci` before artifacts are built;
- release builds are CGo-free and cover Linux amd64, Windows amd64, macOS amd64, and macOS arm64;
- release binaries embed the tag, exact release commit, and a UTC build timestamp derived from that commit;
- the same tested packaging script is exercised by normal CI and by the tag workflow;
- the artifact set contains four platform archives plus one SHA-256 checksum manifest;
- downloaded artifacts are checksum-verified again in the publish job before `gh release create`;
- build/verification runs with read-only repository permission; only the downstream publish job receives `contents: write`;
- GitHub Actions dependencies used by the release workflow are pinned by commit SHA;
- the publish job has explicit repository context and does not depend on a source checkout;
- the workflow never creates, moves, or pushes Git refs;
- a non-empty root `LICENSE` must exist in the tagged source or the workflow fails closed before build/publish.

## Test-driven evidence

The release surface was built through explicit RED/GREEN cycles. Key evidence:

| Contract | RED | GREEN |
|---|---|---|
| development `--version` | `08b8c3dd` / CI `34271200714` | `bf68dcf5` / CI `34271275982` |
| linker-injected metadata | `601759ff` / CI `34271442126` | `f73747fe` / CI `34271548781` |
| release workflow exists and is gated | `422197ff` / CI `34271788931` | implementation followed under permanent CI |
| least-privilege build/publish split | `0648128c` / CI `34272325012` | `41d23e81` / CI `34272478631` |
| explicit `GH_REPO` in checkout-free publish job | `f424e710` / CI `34272797715` | `1e9cd3d4` / CI `34272904705` |
| dereferenced tag commit provenance | `4aa598e4` / CI `34273190591` | `48b316b7` / CI `34273329339` |
| real four-platform packaging in permanent CI | `4ddeab0f` / CI `34273489429` | `1ad6f7f5` / CI `34273623871` |
| workflow delegates to the tested packager | `d2fc2c04` / CI `34273881020` | integrated with the packaging-path correction below |
| relative release output path | `a538c188` / CI `34273919500` | `c1c4621d` / CI `34274114559` |
| downloaded artifact checksum verification | `3eebb1ed` / CI `34274367865` | `f86a87ad` / CI `34274475897` |
| release blocked without root license | `3f276c1d` / CI `34274729560` | `c55f9ae1` / CI `34274859942` |

The exact final PR head and its CI result must be recorded in PR #14 after documentation-only alignment is complete.

## Explicit release boundary

Release-hardening code may be merged to `main` without choosing a license because the release workflow itself fails closed when `LICENSE` is missing or empty. This keeps the legal/license decision separate from mechanical release hardening.

Before creating `v0.1.0`, a maintainer must separately:

1. choose and commit the intended root `LICENSE`;
2. verify the release-hardening changes are present on `main` and post-merge `main` CI is green;
3. select the exact verified `main` commit to release;
4. create and push the `v0.1.0` tag explicitly;
5. verify the tag-triggered release workflow and published checksums/artifacts.

No step in PR #14 creates `v0.1.0`, pushes a tag, or creates a GitHub Release. Those remain consequential maintainer actions outside this readiness change.
